//write a program to check given integer value are same or differnt.
#include <stdio.h>
void main()
{
    int rupesh, harry;
    printf("enter rupesh ammount : ");
    scanf("%d", &rupesh);
    printf("enter harry ammount : ");
    scanf("%d", &harry);
    rupesh == harry ? printf("amount are same") : printf("amount are different");
}